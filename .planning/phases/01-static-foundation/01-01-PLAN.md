---
phase: 01-static-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
  - docs/projects/calculator/index.html
  - docs/projects/calculator/img/logo_2.png
  - .gitignore
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "A docs/ directory exists with an index.html that will be served by GitHub Pages"
    - "The calculator frontend lives under docs/projects/calculator/ with its own index.html"
    - "The portfolio index.html is a minimal working page (not a 404)"
    - "Adding a new project requires only creating a new folder under docs/projects/ and editing the portfolio index.html"
  artifacts:
    - path: "docs/index.html"
      provides: "Portfolio shell landing page"
      contains: "<!DOCTYPE html>"
    - path: "docs/projects/calculator/index.html"
      provides: "Calculator frontend separated from Flask server"
      contains: "True Value Finder"
    - path: "docs/projects/calculator/img/logo_2.png"
      provides: "Calculator logo asset copied to new location"
  key_links:
    - from: "docs/projects/calculator/index.html"
      to: "https://jun-xian-project.onrender.com"
      via: "API constant pointing to Render"
      pattern: "jun-xian-project\\.onrender\\.com"
---

<objective>
Restructure the repository so GitHub Pages can serve a static portfolio site from the docs/ directory, with the calculator frontend separated from the Flask backend into its own project subfolder.

Purpose: Establish the file structure that GitHub Pages will deploy, separating the static portfolio shell from individual project assets so future projects can be added by creating a new folder and a card entry.

Output: docs/index.html (portfolio shell), docs/projects/calculator/index.html (calculator frontend), updated .gitignore
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/ directory structure and move calculator frontend</name>
  <files>docs/index.html, docs/projects/calculator/index.html, docs/projects/calculator/img/logo_2.png</files>
  <read_first>
    - index.html (the existing calculator frontend -- this becomes docs/projects/calculator/index.html)
    - img/logo_2.png (logo asset to copy into docs/projects/calculator/img/)
  </read_first>
  <action>
1. Create directory structure:
   - docs/
   - docs/projects/
   - docs/projects/calculator/
   - docs/projects/calculator/img/

2. Create docs/projects/calculator/index.html by copying the existing root index.html with ONE change:
   - Replace the API constant block (lines 707-709) so it ALWAYS points to Render in production:
     ```javascript
     const API = (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1')
       ? 'http://localhost:5000'
       : 'https://jun-xian-project.onrender.com';
     ```
     This block stays THE SAME -- it already handles both local dev and production correctly.
   - All other content remains identical to the original index.html.

3. Copy img/logo_2.png to docs/projects/calculator/img/logo_2.png (the calculator references this logo).

4. Create docs/index.html as a minimal portfolio landing page with this exact content:
   ```html
   <!DOCTYPE html>
   <html lang="en">
   <head>
     <meta charset="UTF-8">
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>Portfolio</title>
     <style>
       *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
       :root {
         --bg: #0d1117;
         --surface: #161b22;
         --border: #30363d;
         --accent: #58a6ff;
         --text: #c9d1d9;
         --text-bright: #f0f6fc;
         --text-dim: #8b949e;
         --radius: 8px;
       }
       body {
         font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Helvetica, Arial, sans-serif;
         background: var(--bg);
         color: var(--text);
         line-height: 1.6;
         min-height: 100vh;
       }
       header {
         background: var(--surface);
         border-bottom: 1px solid var(--border);
         padding: 14px 24px;
         display: flex;
         align-items: center;
         gap: 12px;
       }
       header img { width: 34px; height: 34px; border-radius: 8px; }
       header h1 { font-size: 17px; font-weight: 600; color: var(--text-bright); }
       main {
         max-width: 900px;
         margin: 60px auto;
         padding: 0 24px;
         text-align: center;
       }
       main h2 { font-size: 28px; color: var(--text-bright); margin-bottom: 12px; }
       main p { color: var(--text-dim); margin-bottom: 40px; }
       .projects { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 20px; }
       .card {
         background: var(--surface);
         border: 1px solid var(--border);
         border-radius: var(--radius);
         padding: 24px;
         text-align: left;
         text-decoration: none;
         color: var(--text);
         transition: border-color 0.2s;
       }
       .card:hover { border-color: var(--accent); }
       .card h3 { color: var(--text-bright); margin-bottom: 8px; }
       .card p { color: var(--text-dim); font-size: 14px; margin-bottom: 12px; }
       .tags { display: flex; gap: 6px; flex-wrap: wrap; }
       .tag {
         background: var(--bg);
         border: 1px solid var(--border);
         border-radius: 12px;
         padding: 2px 10px;
         font-size: 12px;
         color: var(--text-dim);
       }
     </style>
   </head>
   <body>
     <header>
       <img src="projects/calculator/img/logo_2.png" alt="Logo">
       <h1>Portfolio</h1>
     </header>
     <main>
       <h2>Projects</h2>
       <p>Click a card to try the project.</p>
       <div class="projects">
         <a class="card" href="projects/calculator/index.html">
           <h3>Intrinsic Value Calculator</h3>
           <p>Fetch live stock data and calculate intrinsic value using DCF analysis with 20-year projections.</p>
           <div class="tags">
             <span class="tag">Python</span>
             <span class="tag">Flask</span>
             <span class="tag">JavaScript</span>
             <span class="tag">Finance</span>
           </div>
         </a>
       </div>
     </main>
   </body>
   </html>
   ```

This landing page uses the same dark theme CSS variables as the calculator for visual consistency. The card grid uses CSS Grid with `auto-fill` so adding more cards naturally flows. Adding a new project means: (1) create docs/projects/new-project/ with its files, (2) add another `<a class="card">` element inside the `.projects` div.
  </action>
  <verify>
    <automated>ls docs/index.html docs/projects/calculator/index.html docs/projects/calculator/img/logo_2.png 2>&amp;1 &amp;&amp; grep -c "DOCTYPE" docs/index.html &amp;&amp; grep -c "True Value Finder" docs/projects/calculator/index.html &amp;&amp; grep -c "jun-xian-project.onrender.com" docs/projects/calculator/index.html</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.html exists and contains "<!DOCTYPE html>"
    - docs/index.html contains '<div class="projects">' (grid container for project cards)
    - docs/index.html contains 'href="projects/calculator/index.html"' (link to calculator)
    - docs/index.html contains '<span class="tag">Python</span>' (category tags present)
    - docs/projects/calculator/index.html exists and contains "True Value Finder"
    - docs/projects/calculator/index.html contains "jun-xian-project.onrender.com"
    - docs/projects/calculator/img/logo_2.png exists (file size > 0 bytes)
  </acceptance_criteria>
  <done>The docs/ directory exists with a portfolio shell index.html containing a project card grid, and the calculator frontend is at docs/projects/calculator/index.html pointing to the Render API, with its logo asset copied.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore and verify project structure supports extensibility</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore (current ignore rules -- must preserve existing rules)
    - docs/index.html (verify it was created correctly in Task 1)
  </read_first>
  <action>
1. Update .gitignore:
   - Remove the line `img//` (the trailing double-slash typo at line 30)
   - Add a section for the docs/ deployment directory. The img/ directory at root level should NOT be ignored since it now has assets that need to be committed (logo files). Replace the `img//` line with:
     ```
     # Root img is committed (logo assets)
     # docs/ is the GitHub Pages deployment root — always committed
     ```
   - Keep all existing rules (venv/, __pycache__/, *.pyc, *.pyo, .ipynb_checkpoints/, *.xlsx, *.csv, the dev scripts, .DS_Store, Thumbs.db)

2. Verify the directory structure is correct by listing:
   ```
   docs/
   docs/index.html
   docs/projects/
   docs/projects/calculator/
   docs/projects/calculator/index.html
   docs/projects/calculator/img/
   docs/projects/calculator/img/logo_2.png
   ```

3. Verify extensibility: confirm that docs/index.html has a `.projects` grid div where new `<a class="card">` elements can be added, and that docs/projects/ is the parent directory for project subfolders.
  </action>
  <verify>
    <automated>grep -n "img//" .gitignore; echo "Exit: $?" &amp;&amp; ls -R docs/ 2>&amp;1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - .gitignore does NOT contain the line "img//" (the typo is removed)
    - .gitignore still contains "venv/" on its own line
    - .gitignore still contains "__pycache__/" on its own line
    - docs/ directory contains index.html at root level
    - docs/projects/ directory exists
    - docs/projects/calculator/ directory exists with index.html inside
    - docs/projects/calculator/img/ directory exists with logo_2.png inside
  </acceptance_criteria>
  <done>.gitignore is cleaned up, img/ directory is no longer ignored, and the docs/ structure is verified as ready for GitHub Pages deployment with clear extensibility (add folder + card to expand).</done>
</task>

</tasks>

<verification>
1. `ls docs/index.html` -- portfolio shell exists
2. `ls docs/projects/calculator/index.html` -- calculator frontend exists in project subfolder
3. `grep "jun-xian-project.onrender.com" docs/projects/calculator/index.html` -- API URL points to Render
4. `grep "projects/calculator/index.html" docs/index.html` -- portfolio links to calculator
5. `grep -c "img//" .gitignore` returns 0 -- typo removed
</verification>

<success_criteria>
- docs/index.html loads in a browser showing a portfolio page with at least one project card
- docs/projects/calculator/index.html is a complete copy of the calculator frontend
- The file structure under docs/ follows the pattern: docs/projects/{project-name}/ for each project
- .gitignore no longer blocks the img/ directory from being committed
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-foundation/01-01-SUMMARY.md`
</output>
