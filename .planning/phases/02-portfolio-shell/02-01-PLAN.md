---
phase: 02-portfolio-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/index.html
autonomous: false
requirements:
  - HOME-01
  - HOME-02
  - SHOW-01
  - SHOW-02

must_haves:
  truths:
    - "Logo is visible in the top-left header area"
    - "Hero section displays a name heading, AI Engineer title, bio paragraph, and LinkedIn link"
    - "Projects section heading appears left-aligned above the card grid"
    - "Project card grid is visible with at least one card"
    - "Calculator card shows a thumbnail image (or CSS placeholder) above the title"
    - "Card still shows title, description, and category tags"
  artifacts:
    - path: "docs/index.html"
      provides: "Complete portfolio home page with hero and card thumbnails"
      contains: "class=\"hero\""
  key_links:
    - from: "docs/index.html .hero"
      to: "LinkedIn profile"
      via: "anchor href"
      pattern: "linkedin\\.com/in/"
    - from: "docs/index.html .card img"
      to: "docs/projects/calculator/thumbnail.png"
      via: "img src attribute"
      pattern: "projects/calculator/thumbnail\\.png"
---

<objective>
Build the portfolio home page by extending docs/index.html with a centered hero section (name, title, bio, LinkedIn link) and thumbnail support on project cards.

Purpose: Deliver a professional landing experience so visitors immediately understand who the developer is and can browse projects visually.
Output: Updated docs/index.html with hero section, section label, card thumbnail CSS, and placeholder thumbnail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-portfolio-shell/02-CONTEXT.md
@.planning/phases/02-portfolio-shell/02-UI-SPEC.md
@.planning/phases/02-portfolio-shell/02-RESEARCH.md

<interfaces>
<!-- Current state of docs/index.html — the only file modified in this plan -->
<!-- Executor MUST read the full file before editing -->

Existing CSS variables (from :root):
```css
--bg: #0d1117;
--surface: #161b22;
--border: #30363d;
--accent: #58a6ff;
--text: #c9d1d9;
--text-bright: #f0f6fc;
--text-dim: #8b949e;
--radius: 8px;
```

Existing HTML structure (body):
```html
<header>
  <img src="projects/calculator/img/logo_2.png" alt="Logo">
  <h1>Portfolio</h1>
</header>
<main>
  <h2>Projects</h2>
  <p>Click a card to try the project.</p>
  <div class="projects">
    <a class="card" href="projects/calculator/index.html">
      <h3>Intrinsic Value Calculator</h3>
      <p>...</p>
      <div class="tags">...</div>
    </a>
  </div>
</main>
```

Existing CSS rules that will conflict with new hero content:
- `main h2 { font-size: 28px; color: var(--text-bright); margin-bottom: 12px; }` — reusable for hero h2
- `main p { color: var(--text-dim); margin-bottom: 40px; }` — CONFLICTS with .hero-bio (wrong color, wrong margin)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hero section, section label, and card thumbnail support to docs/index.html</name>
  <files>docs/index.html</files>
  <read_first>
    - docs/index.html (current full file — MUST read before editing)
    - .planning/phases/02-portfolio-shell/02-UI-SPEC.md (visual contract — authoritative source for all CSS values)
    - .planning/phases/02-portfolio-shell/02-CONTEXT.md (locked decisions D-01 through D-07)
    - .planning/phases/02-portfolio-shell/02-RESEARCH.md (pitfalls section — critical)
  </read_first>
  <action>
Extend docs/index.html in-place. Do NOT rewrite from scratch. Make these specific changes:

**1. Add new CSS rules inside the existing `<style>` block, AFTER all existing rules (cascade order matters for specificity):**

```css
/* Hero section — per D-01 centered layout */
.hero {
  text-align: center;
  margin-bottom: 48px;
}
.hero h2 {
  font-size: 28px;
  font-weight: 600;
  color: var(--text-bright);
  line-height: 1.2;
  margin-bottom: 8px;
}
.hero-title {
  font-size: 16px;
  font-weight: 400;
  color: var(--accent);
  margin-bottom: 16px;
}
/* IMPORTANT: .hero-bio MUST appear AFTER the broad `main p` rule to override
   its color (--text-dim) and margin-bottom (40px). See RESEARCH.md Pitfall 1. */
.hero-bio {
  font-size: 16px;
  font-weight: 400;
  color: var(--text);
  line-height: 1.6;
  max-width: 560px;
  margin: 0 auto 16px;
}
.hero a {
  font-size: 16px;
  color: var(--accent);
  text-decoration: none;
}
.hero a:hover { text-decoration: underline; }

/* Section label — replaces h2 Projects heading */
.section-label {
  font-size: 16px;
  font-weight: 600;
  color: var(--text-bright);
  text-align: left;
  margin-bottom: 16px;
}

/* Card thumbnail image — per D-05, D-07 */
.card img {
  width: 100%;
  aspect-ratio: 16 / 9;
  object-fit: cover;
  border-radius: 4px;
  margin-bottom: 16px;
  display: block;
}

/* Card thumbnail placeholder — CSS-only fallback when screenshot absent */
.card-thumbnail-placeholder {
  width: 100%;
  aspect-ratio: 16 / 9;
  background: linear-gradient(135deg, #1c2128, #30363d);
  border-radius: 4px;
  margin-bottom: 16px;
}
```

**2. Replace the `<main>` HTML content.** Remove the existing:
```html
<h2>Projects</h2>
<p>Click a card to try the project.</p>
```

Replace with:
```html
<section class="hero">
  <h2><!-- TODO: Replace with your actual name --></h2>
  <p class="hero-title">AI Engineer</p>
  <p class="hero-bio">I spent years doing graph analytics and working closely with the Neo4j team as a data analyst. Now I'm building AI-powered tools — things I actually use and want to share. This is where they live.</p>
  <a href="https://linkedin.com/in/YOUR-HANDLE" target="_blank" rel="noopener noreferrer">LinkedIn</a>
  <!-- TODO: Replace YOUR-HANDLE with actual LinkedIn username -->
</section>
<h3 class="section-label">Projects</h3>
```

This eliminates the duplicate h2 issue (RESEARCH.md Pitfall 2) — the hero h2 is now the only h2 on the page. The "Projects" label becomes an h3 with class="section-label" and text-align: left overrides the main center alignment (Pitfall 3).

**3. Add thumbnail placeholder as first child inside the existing `.card` element:**

Change the card from:
```html
<a class="card" href="projects/calculator/index.html">
  <h3>Intrinsic Value Calculator</h3>
```

To:
```html
<a class="card" href="projects/calculator/index.html">
  <div class="card-thumbnail-placeholder" aria-label="Intrinsic Value Calculator thumbnail — screenshot coming soon"></div>
  <h3>Intrinsic Value Calculator</h3>
```

Use the CSS placeholder div (not an img tag) because the file docs/projects/calculator/thumbnail.png does not exist yet. When the user provides the screenshot in Task 2, the placeholder div will be swapped for a real img tag.

**4. Do NOT change any existing CSS rules** (header, body, :root, .card, .card h3, .card p, .tags, .tag). Only ADD new rules. The existing `main h2` rule will naturally apply to `.hero h2` — the `.hero h2` rule added above provides the overrides needed (line-height, margin-bottom differ from the existing rule).

Per D-03: No skills/technologies list in the hero. Bio paragraph + LinkedIn link only.
Per D-04: LinkedIn only — no email, GitHub, Twitter, or other links.
  </action>
  <verify>
    <automated>grep -c "class=\"hero\"" docs/index.html && grep -c "hero-bio" docs/index.html && grep -c "section-label" docs/index.html && grep -c "card-thumbnail-placeholder" docs/index.html && grep -c "aspect-ratio: 16 / 9" docs/index.html</automated>
  </verify>
  <acceptance_criteria>
    - docs/index.html contains exactly one `<section class="hero">` element
    - docs/index.html contains `<h2>` inside .hero section (hero name heading)
    - docs/index.html contains `<p class="hero-title">AI Engineer</p>`
    - docs/index.html contains `<p class="hero-bio">` with the bio text starting with "I spent years"
    - docs/index.html contains `<a href="https://linkedin.com/in/` (LinkedIn link)
    - docs/index.html contains `<h3 class="section-label">Projects</h3>` (NOT an h2)
    - docs/index.html does NOT contain a bare `<h2>Projects</h2>` (old heading removed)
    - docs/index.html contains `<div class="card-thumbnail-placeholder"` with aria-label inside the .card element
    - docs/index.html contains CSS rule `.hero-bio` with `color: var(--text)` (NOT --text-dim)
    - docs/index.html contains CSS rule `.hero-bio` with `margin: 0 auto 16px` (NOT margin-bottom: 40px)
    - docs/index.html contains CSS rule `.card img` with `aspect-ratio: 16 / 9` and `object-fit: cover`
    - docs/index.html contains CSS rule `.card-thumbnail-placeholder` with `aspect-ratio: 16 / 9`
    - docs/index.html contains CSS rule `.section-label` with `text-align: left`
    - The h2 element count in the entire file is exactly 1 (inside .hero only)
    - All existing CSS rules (header, body, :root, .card, .tags, .tag) are preserved unchanged
    - No hardcoded hex color values in new CSS — only var(--*) references (except the placeholder gradient which uses #1c2128 and #30363d per spec)
  </acceptance_criteria>
  <done>docs/index.html contains hero section with name placeholder, AI Engineer title, bio text, LinkedIn link, section-label h3, and card thumbnail placeholder — all using existing CSS variables and matching UI-SPEC exactly</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User provides personal details and verifies visual output</name>
  <files>docs/index.html</files>
  <action>
User visually verifies the completed portfolio home page and fills in personal details that only they can provide.

What was built: Complete portfolio home page with hero section (name placeholder, AI Engineer title, bio, LinkedIn link), Projects section label, and card with CSS thumbnail placeholder. The layout, typography, colors, and spacing all follow the UI-SPEC contract.

How to verify:
1. Open docs/index.html in your browser
2. Verify the logo appears in the top-left header (HOME-01)
3. Verify the hero section is centered with:
   - A name heading (currently shows TODO placeholder — you will provide your name)
   - "AI Engineer" title in accent blue (#58a6ff)
   - Bio paragraph in --text color (#c9d1d9), NOT --text-dim
   - "LinkedIn" link in accent blue (#58a6ff)
4. Verify "Projects" label appears left-aligned above the card grid
5. Verify the calculator card shows:
   - A gradient placeholder rectangle (16:9 ratio) at the top
   - Title "Intrinsic Value Calculator"
   - Description text
   - Python, Flask, JavaScript, Finance tags
6. Hover over the card — border should turn accent blue
7. Hover over the LinkedIn link — underline should appear

Action items for user:
- Replace the TODO comment in the h2 with your actual name
- Replace YOUR-HANDLE in the LinkedIn href with your actual LinkedIn username
- (Optional now) Take a screenshot of the calculator at ~800x450px, save as docs/projects/calculator/thumbnail.png, then swap the placeholder div for an img tag

Resume signal: Type "approved" after filling in your name and LinkedIn URL, or describe any issues.
  </action>
  <verify>
    <automated>grep -c "linkedin.com/in/" docs/index.html</automated>
  </verify>
  <done>User has confirmed visual output matches expectations, filled in their real name and LinkedIn URL, and approved the page layout</done>
</task>

</tasks>

<verification>
- Open docs/index.html in browser — hero section visible with bio and LinkedIn link
- Only one h2 on the page (hero name) — no duplicate h2 elements
- Card has thumbnail placeholder with 16:9 aspect ratio
- Section label "Projects" is left-aligned
- All colors use CSS variables from :root (no hardcoded hex except placeholder gradient)
- Card hover still works (border turns accent blue)
</verification>

<success_criteria>
- HOME-01: Logo visible in header top-left (already exists, preserved)
- HOME-02: Hero section shows name, AI Engineer title, bio paragraph, LinkedIn link (per D-01, D-02, D-03, D-04)
- SHOW-01: Project card grid visible with at least one card
- SHOW-02: Calculator card shows thumbnail placeholder, title, description, and category tags
</success_criteria>

<output>
After completion, create `.planning/phases/02-portfolio-shell/02-01-SUMMARY.md`
</output>
