---
phase: 07-detection-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-dev.txt
  - pytest.ini
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_detector_schema.py
  - scripts/pattern_scanner/__init__.py
  - scripts/pattern_scanner/detector.py
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04]
tags: [python, detection, pytest, finance, candlestick]
must_haves:
  truths:
    - "scripts/pattern_scanner/detector.py exposes detect(df: pd.DataFrame, ticker: str) -> list[Detection]"
    - "Detection is a frozen dataclass with all D-10 fields and a to_dict() method"
    - "Confirmation classifiers (_is_pin, _is_mark_up, _is_ice_cream) match D-01..D-03 thresholds exactly"
    - "_inside_bar uses strict less-than/greater-than per D-04"
    - "ATR(14) uses Wilder smoothing via series.ewm(alpha=1/14, adjust=False, min_periods=14).mean()"
    - "All trend filters compute on df.iloc[:confirmation_idx + 1] slice — no future bars referenced"
    - "Spring case (break_idx == confirmation_idx) is detected and is_spring=True is recorded"
    - "Per-filter booleans (hh_hl, above_50sma, sma_cluster) are always recorded; AND combination only gates emission"
    - "CLI ticker arg is validated with regex ^[A-Z0-9.-]{1,10}$ before being passed to yfinance"
    - "Running python -m pytest tests/test_detector_schema.py -q exits 0 with all DET-01..DET-04 unit tests green"
  artifacts:
    - path: "scripts/pattern_scanner/__init__.py"
      provides: "Package marker; exports Detection and detect"
    - path: "scripts/pattern_scanner/detector.py"
      provides: "Detection dataclass + detect() public API + private helpers + __main__ CLI"
      contains: "class Detection"
    - path: "tests/__init__.py"
      provides: "tests package marker"
    - path: "tests/conftest.py"
      provides: "--no-network option, network marker auto-skip, shared synthetic OHLC fixtures"
      contains: "pytest_addoption"
    - path: "tests/test_detector_schema.py"
      provides: "Unit tests covering DET-01..DET-04 (no network required)"
      contains: "test_pin_bar_classifier"
    - path: "pytest.ini"
      provides: "testpaths=tests, registers network marker"
      contains: "testpaths = tests"
    - path: "requirements-dev.txt"
      provides: "Dev-only dependencies (pytest)"
      contains: "pytest"
  key_links:
    - from: "scripts/pattern_scanner/detector.py::detect"
      to: "_compute_atr / _compute_sma / _hh_hl_uptrend / _sma_cluster"
      via: "Function calls inside the pattern scan loop, each on df.iloc[:conf_idx + 1]"
      pattern: "df\\.iloc\\[:\\s*\\w*\\s*\\+\\s*1\\]"
    - from: "scripts/pattern_scanner/detector.py::__main__"
      to: "yfinance Ticker.history"
      via: "_fetch_ohlc helper using yf.Ticker(ticker).history(period='10y', auto_adjust=True)"
      pattern: "yf\\.Ticker\\(\\w+\\)\\.history"
    - from: "tests/test_detector_schema.py"
      to: "scripts.pattern_scanner.detector"
      via: "from scripts.pattern_scanner.detector import detect, Detection"
      pattern: "from scripts\\.pattern_scanner\\.detector import"
---

<objective>
Implement the algorithmic 5-bar inside bar spring detector as a pure-Python module at `scripts/pattern_scanner/detector.py`, plus a first-class `tests/` harness that locks every D-01..D-14 rule in unit tests.

Purpose: Phase 7 is the foundation that downstream phases (8 training annotation, 9 backtesting, 10 batch pipeline, 11 frontend) depend on directly. The detector encodes locked finance domain knowledge (confirmation bar definitions, inside-bar rule, HH/HL trend filter, SMA-cluster filter, spring case) and must produce the same answer regardless of how the input frame was truncated (no look-ahead).

Output: A package `scripts/pattern_scanner/` with `detector.py` exposing `detect(df, ticker) -> list[Detection]`, a `tests/` directory at repo root with `pytest.ini`, `conftest.py`, and `test_detector_schema.py` (all DET-01..DET-04 covered by unit tests), and a `requirements-dev.txt` for dev deps. CLI invocation `python scripts/pattern_scanner/detector.py AAPL` works end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-detection-engine/07-CONTEXT.md
@.planning/phases/07-detection-engine/07-RESEARCH.md
@.planning/phases/07-detection-engine/07-VALIDATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@./CLAUDE.md
@yahoo_finance_fetcher.py
@scripts/fetch_sp500.py

<interfaces>
<!-- Contracts the executor must implement / consume. Copy verbatim into code. -->

# Detection dataclass (D-10, D-11) — frozen dataclass with to_dict()
```python
from dataclasses import dataclass, asdict
from typing import List, Dict

@dataclass(frozen=True)
class Detection:
    ticker: str
    confirmation_date: str          # ISO YYYY-MM-DD
    confirmation_type: str          # "pin" | "mark_up" | "ice_cream"
    is_spring: bool
    bars: List[Dict]                # 5 dicts: {date, open, high, low, close} mother → confirmation
    mother_bar_index: int
    confirmation_bar_index: int
    filters: Dict[str, bool]        # {hh_hl, above_50sma, sma_cluster}
    sma_levels: Dict[str, float]    # {sma20, sma50, atr14}

    def to_dict(self) -> Dict:
        return asdict(self)
```

# Public API (D-12) — exact signature
```python
def detect(df: pd.DataFrame, ticker: str) -> list[Detection]:
    """Detect bullish inside bar spring setups in daily OHLC data.
    df: DataFrame with flat columns Open/High/Low/Close and DatetimeIndex (tz-naive).
    ticker: uppercase symbol; included in each Detection record.
    """
```

# Confirmation predicates (D-01..D-03) — copy formulas verbatim
- Pin (D-01):       open >= L + (2/3)*(H-L) AND close >= L + (2/3)*(H-L)
- Mark-up (D-02):   (close - open) >= (2/3)*(H-L) AND close > open
- Ice-cream (D-03): min(open, close) <= L + 0.5*(H-L) AND close >= L + (1/3)*(H-L) AND close > open
- Inside (D-04):    H_inside < H_mother AND L_inside > L_mother (STRICT)
- Precedence in _classify_confirmation: pin → mark_up → ice_cream

# Trend filters (D-05..D-08)
- HH/HL (D-05): 5-bar fractal swing pivots over 60-bar lookback ending at conf_idx; pass when last 2 swing highs ascending AND last 2 swing lows ascending. Strict `>` / `<` for pivots.
- Above 50-SMA (D-06): close[conf_idx] > sma_50.iloc[-1] computed on df.iloc[:conf_idx + 1]
- SMA cluster (D-07): mother_bar_low within ±1 × ATR(14) of EITHER 20-SMA OR 50-SMA at the mother bar's index, all computed on df.iloc[:mother_idx + 1]
- Combination (D-08): All three booleans recorded; emission requires strict AND

# ATR (Wilder) — one-line idiom
```python
atr = tr.ewm(alpha=1/14, adjust=False, min_periods=14).mean()
```
where tr = max(H-L, |H-prev_close|, |L-prev_close|) row-wise.

# CLI security control (T-07-01)
```python
import re
TICKER_RE = re.compile(r"^[A-Z0-9.-]{1,10}$")
if not TICKER_RE.match(ticker):
    raise SystemExit(f"Invalid ticker: {ticker!r}")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 scaffolding — package skeleton, pytest infra, schema unit tests, Detection dataclass + detect() signature</name>
  <files>
    requirements-dev.txt,
    pytest.ini,
    tests/__init__.py,
    tests/conftest.py,
    tests/test_detector_schema.py,
    scripts/pattern_scanner/__init__.py,
    scripts/pattern_scanner/detector.py
  </files>
  <read_first>
    .planning/phases/07-detection-engine/07-CONTEXT.md,
    .planning/phases/07-detection-engine/07-RESEARCH.md,
    .planning/phases/07-detection-engine/07-VALIDATION.md,
    .planning/codebase/CONVENTIONS.md,
    yahoo_finance_fetcher.py,
    scripts/fetch_sp500.py,
    ./CLAUDE.md
  </read_first>
  <behavior>
    - Test 1 (test_pin_bar_classifier): a synthetic bar with open=L+0.7*range and close=L+0.8*range returns True; bar with close=L+0.5*range returns False.
    - Test 2 (test_mark_up_bar_classifier): a bullish bar with body = 0.7*range returns True; a bearish bar (close < open) with same body magnitude returns False; a bullish bar with body = 0.5*range returns False.
    - Test 3 (test_ice_cream_bar_classifier): a bullish bar with min(open,close) at L + 0.4*range and close at L + 0.6*range returns True; a bearish bar in the same shape returns False.
    - Test 4 (test_inside_bar_rule): mother [H=110, L=100], child [H=109, L=101] → True; child [H=110, L=101] → False (strict).
    - Test 5 (test_atr14_wilder): on a synthetic 30-bar series, ATR(14) values match the recursive Wilder formula and first 13 values are NaN.
    - Test 6 (test_swing_pivots): on a synthetic series with known peaks/troughs, _swing_pivots returns the expected indices.
    - Test 7 (test_sma_cluster): mother_bar_low within ±1·ATR of 20-SMA returns True via _sma_cluster.
    - Test 8 (test_detect_returns_detection_list): detect() on a 100-bar synthetic frame engineered to contain one full 5-bar setup returns a list with one Detection whose confirmation_type, is_spring, and indices match expectations.
    - Test 9 (test_spring_same_bar): detect() on a synthetic frame engineered with break-below == confirmation returns a Detection with is_spring=True.
    - Test 10 (test_no_lookahead_truncation_invariance): detect(df.iloc[:k+1]) emits the same detections (with confirmation_idx <= k) as detect(df) does for those same indices.
    - Test 11 (test_detection_record_schema): every key in D-10 is present on Detection; to_dict() round-trips via json.dumps.
    - Test 12 (test_cli_ticker_validation): invoking the module's main() with ticker "rm -rf /" exits non-zero; with "AAPL" proceeds (mock yfinance to avoid network).
  </behavior>
  <action>
    Create the full Wave 0 scaffold and the entire detection module in ONE pass. The scaffold has no algorithmic risk — every threshold and formula is locked in CONTEXT.md / RESEARCH.md. Produce:

    **requirements-dev.txt** — single line: `pytest>=8.4,<9`. Do NOT add to requirements.txt (runtime stays minimal per D-13 / RESEARCH "no new runtime deps").

    **pytest.ini** at repo root:
    ```ini
    [pytest]
    testpaths = tests
    markers =
        network: tests that require live yfinance / network access
    ```

    **tests/__init__.py** — empty file (package marker).

    **tests/conftest.py** — implement per RESEARCH Example 5:
    - `pytest_addoption(parser)` adds `--no-network` flag (action=store_true, default=False)
    - `pytest_configure(config)` registers the `network` marker via `config.addinivalue_line("markers", "network: ...")`
    - `pytest_collection_modifyitems(config, items)` skips tests with the `network` marker when `--no-network` is set OR when env var `PYTEST_OFFLINE=1`
    - Add a fixture `synthetic_ohlc(request)` that builds small DataFrames with deterministic OHLC for unit tests (helper used by tests in Task 1). Function signature: `synthetic_ohlc(rows: list[tuple[float,float,float,float]], start_date: str = "2024-01-01") -> pd.DataFrame` returning a DataFrame with `Open/High/Low/Close` columns and a tz-naive DatetimeIndex on US business days.

    **scripts/pattern_scanner/__init__.py** — re-export the public API:
    ```python
    from .detector import Detection, detect
    __all__ = ["Detection", "detect"]
    ```

    **scripts/pattern_scanner/detector.py** — implement the FULL module. Sections in order:

    1. Module docstring describing purpose, public API, no-look-ahead invariant.
    2. Imports: `from __future__ import annotations`; stdlib (`json`, `math`, `re`, `sys`); third-party (`numpy as np`, `pandas as pd`); typing (`from dataclasses import dataclass, asdict, field`; `from typing import List, Dict, Optional, Tuple`). Note: import yfinance only inside `_fetch_ohlc` (deferred import keeps unit tests fast).
    3. Module constants: `_LOOKBACK = 60`, `_ATR_PERIOD = 14`, `_SMA20 = 20`, `_SMA50 = 50`, `_TICKER_RE = re.compile(r"^[A-Z0-9.-]{1,10}$")`.
    4. `Detection` dataclass per D-10/D-11 (frozen=True; `to_dict()` returns `asdict(self)`).
    5. Confirmation classifiers — copy formulas from CONTEXT verbatim:
       - `_is_pin(bar)`: `open >= L + (2/3)*(H-L) AND close >= L + (2/3)*(H-L)`. Guard `rng <= 0` returns False.
       - `_is_mark_up(bar)`: `(close - open) >= (2/3)*(H-L) AND close > open`. Guard rng > 0.
       - `_is_ice_cream(bar)`: `min(open, close) <= L + 0.5*(H-L) AND close >= L + (1/3)*(H-L) AND close > open`. Guard rng > 0.
       - `_classify_confirmation(bar) -> Optional[str]`: precedence pin → mark_up → ice_cream.
    6. `_inside_bar(mother, child) -> bool`: `child["High"] < mother["High"] and child["Low"] > mother["Low"]` (strict, per D-04).
    7. Indicators (slice-first idiom — never index full-frame results):
       - `_compute_sma(close: pd.Series, period: int) -> pd.Series`: `close.rolling(window=period, min_periods=period).mean()`.
       - `_compute_atr(view: pd.DataFrame, period: int = 14) -> pd.Series`: build TR via `pd.concat([H-L, (H-prev_close).abs(), (L-prev_close).abs()], axis=1).max(axis=1)` then `tr.ewm(alpha=1/period, adjust=False, min_periods=period).mean()`. Add docstring "Wilder-smoothed ATR; caller must pre-slice."
    8. Swing pivots (D-05):
       - `_swing_pivots(view: pd.DataFrame) -> Tuple[list[int], list[int]]` per RESEARCH Pattern 3 (strict `>` / `<` on bars i-2, i-1, i+1, i+2).
       - `_hh_hl_uptrend(view: pd.DataFrame) -> bool`: returns False if fewer than 2 swing highs OR 2 swing lows; else `last_high > prev_high AND last_low > prev_low`.
    9. SMA cluster filter (D-07):
       - `_sma_cluster(df: pd.DataFrame, mother_idx: int) -> bool`: slice `view = df.iloc[:mother_idx + 1]`; compute sma20, sma50, atr14 (each `.iloc[-1]`); return `(abs(mother_low - sma20) <= atr14) OR (abs(mother_low - sma50) <= atr14)`. Return False if any indicator is NaN.
    10. Detection assembly:
        - `_build_detection(df, ticker, mother_idx, break_idx, conf_idx, conf_type)`: slice `view = df.iloc[:conf_idx + 1]`; compute hh_hl, above_50sma, sma_cluster booleans; gather sma_levels (sma20, sma50, atr14 at conf_idx); build `bars` as the 5-row window (mother..mother+4 truncated to n) with `{date: idx.strftime("%Y-%m-%d"), open, high, low, close}`; return Detection. Per D-08, ALL THREE filter booleans must always be evaluated regardless of short-circuit.
    11. `detect(df: pd.DataFrame, ticker: str) -> list[Detection]` — pattern scan loop per RESEARCH Pattern 4:
        - Defensive asserts: required columns present, DatetimeIndex tz-naive (`df.index.tz is None`).
        - `if len(df) < _LOOKBACK: return []`.
        - `for mother_idx in range(_LOOKBACK, len(df) - 4):`
          - skip unless `_inside_bar(df.iloc[mother_idx], df.iloc[mother_idx + 1])`
          - `mother_low = df.iloc[mother_idx]["Low"]`
          - `for break_offset in range(2, 5):` (offsets 2,3,4 from mother)
            - `break_idx = mother_idx + break_offset`; bounds-check
            - skip unless `df.iloc[break_idx]["Low"] < mother_low`
            - `for conf_idx in range(break_idx, min(mother_idx + 5, len(df))):`
              - `conf_type = _classify_confirmation(df.iloc[conf_idx])`
              - skip if conf_type is None
              - skip unless `mother_low < df.iloc[conf_idx]["Close"] < df.iloc[mother_idx]["High"]`
              - build detection with `is_spring = (break_idx == conf_idx)`
              - emit only if all three filter booleans True
              - `break` (one confirmation per break-below)
            - `break` (one break-below window per mother)
    12. CLI helper `_fetch_ohlc(ticker: str, period: str = "10y") -> pd.DataFrame`:
        - `import yfinance as yf` (deferred)
        - `df = yf.Ticker(ticker).history(period=period, auto_adjust=True)[["Open", "High", "Low", "Close"]]`
        - `df.index = df.index.tz_localize(None)`
        - `return df`
    13. `__main__` block (D-13 + T-07-01 mitigation):
        - `if len(sys.argv) < 2: print("Usage: python scripts/pattern_scanner/detector.py <TICKER>", file=sys.stderr); sys.exit(1)`
        - `ticker = sys.argv[1].upper()`
        - **`if not _TICKER_RE.match(ticker): print(f"Invalid ticker: {ticker!r}", file=sys.stderr); sys.exit(2)`**  ← T-07-01
        - `df = _fetch_ohlc(ticker)`
        - `detections = detect(df, ticker)`
        - `print(json.dumps([d.to_dict() for d in detections], indent=2, default=str))`
    14. Wrap the validation regex + fetch + detect path into a callable `def main(argv: list[str]) -> int:` so unit tests can invoke it without subprocess; the `if __name__ == "__main__":` block calls `sys.exit(main(sys.argv))`.

    **tests/test_detector_schema.py** — implement ALL 12 unit tests listed in the `<behavior>` block above. NO `@pytest.mark.network` — these are pure-unit tests using synthetic OHLC frames built via the `synthetic_ohlc` fixture. Use precedence test for `_classify_confirmation` (a bar that satisfies both pin and mark_up returns "pin"). For `test_cli_ticker_validation`, use `monkeypatch` on `_fetch_ohlc` to return a synthetic empty DataFrame so the test does not hit the network.

    Constraints:
    - Run all `pip` / `python` / `pytest` commands inside `.venv` (Windows: `source .venv/Scripts/activate` first per MEMORY.md `feedback_venv.md`).
    - Do NOT add `Co-Authored-By: Claude` trailers to any commit (MEMORY.md `feedback_git_attribution.md`).
    - snake_case for files/functions; PascalCase only for `Detection`; leading underscore for every helper that is not part of the public API (per CONVENTIONS.md).
    - Never use `.shift(-1)` anywhere — negative shift is the canonical look-ahead operator and is banned in this module.
    - Do NOT compute SMA/ATR on the full df and then index into the result. Always slice first, then compute, then read `.iloc[-1]`.
    - The Detection dataclass MUST be `frozen=True`; mutating fields after construction must raise.
    - Tests use the synthetic_ohlc fixture and never call yfinance.

    After implementation:
    - `python -m pip install -r requirements-dev.txt` inside `.venv`
    - `python -m pytest tests/test_detector_schema.py -q` — all 12 tests must pass.
  </action>
  <verify>
    <automated>cd C:/Users/zenng/Desktop/portfolio/jun-xian-project &amp;&amp; source .venv/Scripts/activate &amp;&amp; python -m pip install -r requirements-dev.txt &amp;&amp; python -m pytest tests/test_detector_schema.py -q</automated>
  </verify>
  <acceptance_criteria>
    - File `requirements-dev.txt` exists and matches regex `^pytest>=8\.4,<9\s*$`
    - File `pytest.ini` exists and contains lines matching `testpaths\s*=\s*tests` AND `network:` (in markers block)
    - File `tests/__init__.py` exists (may be empty)
    - File `tests/conftest.py` contains `def pytest_addoption`, `--no-network`, `def pytest_configure`, `def pytest_collection_modifyitems`, and a `synthetic_ohlc` fixture
    - File `scripts/pattern_scanner/__init__.py` contains `from .detector import Detection, detect`
    - File `scripts/pattern_scanner/detector.py` contains:
      - `@dataclass(frozen=True)` and `class Detection`
      - `def to_dict(self)`
      - `def detect(df: pd.DataFrame, ticker: str) -> list[Detection]:`
      - `def _is_pin(`, `def _is_mark_up(`, `def _is_ice_cream(`, `def _classify_confirmation(`, `def _inside_bar(`, `def _compute_sma(`, `def _compute_atr(`, `def _swing_pivots(`, `def _hh_hl_uptrend(`, `def _sma_cluster(`, `def _build_detection(`, `def _fetch_ohlc(`, `def main(`
      - `_TICKER_RE = re.compile(r"^[A-Z0-9.-]{1,10}$")`
      - String `ewm(alpha=1/14, adjust=False, min_periods=14)` OR `ewm(alpha=1/_ATR_PERIOD, adjust=False, min_periods=_ATR_PERIOD)`
      - At least three occurrences of `df.iloc[:` followed by an index-plus-one expression (slice-first idiom)
      - String `is_spring` and the assignment `is_spring=(break_idx == conf_idx)` (or equivalent equality check on those locals)
      - NO occurrence of the string `shift(-` (negative shift is banned)
    - File `tests/test_detector_schema.py` contains all of: `def test_pin_bar_classifier`, `def test_mark_up_bar_classifier`, `def test_ice_cream_bar_classifier`, `def test_inside_bar_rule`, `def test_swing_pivots`, `def test_atr14_wilder`, `def test_sma_cluster`, `def test_detect_returns_detection_list`, `def test_spring_same_bar`, `def test_no_lookahead_truncation_invariance`, `def test_detection_record_schema`, `def test_cli_ticker_validation`
    - `python -m pytest tests/test_detector_schema.py -q` exits 0 with 12 passed
    - `python -m pytest --collect-only -q` exits 0 (validates pytest.ini config)
    - `python -c "from scripts.pattern_scanner.detector import Detection, detect; assert hasattr(Detection, 'to_dict')"` exits 0
    - `python -c "import scripts.pattern_scanner.detector as m, sys; sys.exit(0 if m.main(['detector.py', 'rm -rf /']) != 0 else 1)"` exits 0 (CLI rejects bogus ticker)
  </acceptance_criteria>
  <done>
    Detection module + tests committed. All 12 schema unit tests green offline. CLI rejects malformed tickers via regex `^[A-Z0-9.-]{1,10}$`. Live invocation `python scripts/pattern_scanner/detector.py AAPL` (manual smoke) prints JSON detection list (may be empty if no setups in 10y history — that is acceptable for this task; Plan 02 adds the live regression suite).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → yfinance HTTPS request | User-supplied ticker string crosses from process args into a network identifier; bounded length & alphabet limit attack surface |
| yfinance response → DataFrame | Third-party library output reaches detector; treated as data only (no eval, no exec) |
| Test runner → live network | pytest may invoke yfinance in Plan 02 (network-marked tests) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering / Input Validation | `__main__` CLI ticker arg in scripts/pattern_scanner/detector.py | mitigate | Validate against `_TICKER_RE = re.compile(r"^[A-Z0-9.-]{1,10}$")` BEFORE passing to `yf.Ticker(...)`. On mismatch, print to stderr and exit non-zero. Unit test `test_cli_ticker_validation` enforces the gate. |
| T-07-02 | Supply Chain | Dependency CVEs in pandas / numpy / yfinance / pytest | accept | All runtime deps already pinned in `requirements.txt`; pytest pinned `>=8.4,<9` in `requirements-dev.txt`. Run `pip-audit` quarterly out of band — not a Phase 7 task. |
| T-07-03 | Tampering | Path traversal via test fixture filename | mitigate | Tests reference paths only via `Path(__file__).parent` or `tmp_path`; never accept paths from env or argv. Code review check. |
| T-07-04 | Information Disclosure | Detection JSON output leaks secrets | accept | OHLC is public market data; no secrets, no PII, no auth state. Severity: INFO. |
| T-07-05 | Denial of Service | Adversarial ticker forces large yfinance fetch | accept | Length-bounded ticker (max 10) + yfinance internal rate limiting. Severity: LOW. |
</threat_model>

<verification>
1. Wave 0 + module + tests created in a single pass: dependencies installed, pytest collection works.
2. Run: `python -m pytest tests/test_detector_schema.py -q` — must exit 0 with 12 tests passed.
3. Run: `python -m pytest --collect-only -q` — must exit 0 (validates pytest.ini configures testpaths correctly).
4. Smoke run: `python scripts/pattern_scanner/detector.py AAPL` — must produce valid JSON (an array of 0+ Detection records). Network required.
5. Negative smoke: `python scripts/pattern_scanner/detector.py "rm -rf /"` — must exit non-zero with an "Invalid ticker" message (T-07-01 mitigation).
6. Grep audit: `grep -rn "shift(-" scripts/pattern_scanner/` returns no matches (negative-shift ban).
7. Grep audit: `grep -n "df.iloc\[:" scripts/pattern_scanner/detector.py` returns at least three matches (slice-first idiom).
</verification>

<success_criteria>
- `scripts/pattern_scanner/detector.py` implements detect() per D-12 with all helpers private (leading underscore).
- All 12 unit tests in `tests/test_detector_schema.py` pass offline (no network).
- DET-01 (5-bar ruleset) — covered by test_pin/mark_up/ice_cream/inside_bar/detect_returns_detection_list.
- DET-02 (spring case) — covered by test_spring_same_bar.
- DET-03 (no-look-ahead) — covered by test_no_lookahead_truncation_invariance.
- DET-04 (Detection record schema) — covered by test_detection_record_schema.
- T-07-01 (CLI input validation) — enforced by `_TICKER_RE` regex; covered by test_cli_ticker_validation.
- pytest infra (`pytest.ini`, `conftest.py`, `requirements-dev.txt`) installed and functional; first formal `tests/` directory in repo.
- No `Co-Authored-By: Claude` trailer in commits (per MEMORY.md `feedback_git_attribution.md`).
- All commands run inside `.venv` (per MEMORY.md `feedback_venv.md`).
</success_criteria>

<output>
After completion, create `.planning/phases/07-detection-engine/07-01-SUMMARY.md` describing what was implemented, key design choices (Wilder ATR, slice-first idiom, frozen dataclass, CLI ticker regex), test coverage map (DET-01..DET-04 → test names), files modified, and one-line description of the public API for downstream phases (8/9/10/11) to consume directly.
</output>
