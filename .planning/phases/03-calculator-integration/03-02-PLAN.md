---
phase: 03-calculator-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified: []
autonomous: false
requirements:
  - SHOW-03
  - NAV-01
  - NAV-02

must_haves:
  truths:
    - "Calculator fetches stock data from Render API when loaded via iframe on GitHub Pages (no CORS errors)"
    - "The full navigation flow works on the live deployed site identical to local"
  artifacts: []
  key_links:
    - from: "GitHub Pages iframe (projects/calculator/index.html)"
      to: "https://jun-xian-project.onrender.com/api/health"
      via: "fetch() from calculator JS inside iframe"
      pattern: "fetch.*jun-xian-project\\.onrender\\.com"
---

<objective>
Verify the calculator integration works end-to-end on the live GitHub Pages deployment, specifically that CORS allows the iframe-hosted calculator to call the Render API.

Purpose: Per D-08, this is an explicit verification that the deployed site works — not just local. The calculator's API calls must succeed from the GitHub Pages domain without CORS errors.

Output: Confirmed working deployment or identified CORS fix needed (D-09).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-calculator-integration/03-CONTEXT.md
@.planning/phases/03-calculator-integration/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push changes and trigger GitHub Pages deployment</name>
  <files></files>
  <read_first>
    - docs/index.html (confirm Phase 3 changes are committed)
  </read_first>
  <action>
Ensure all docs/index.html changes from Plan 01 are committed and pushed to main branch. GitHub Pages auto-deploys from docs/ on main.

Run:
```bash
git push origin main
```

Then wait for GitHub Pages deployment to complete. Check deployment status:
```bash
gh api repos/{owner}/{repo}/pages/builds/latest --jq '.status'
```

If the push was already done by Plan 01, just verify deployment status.
  </action>
  <verify>
    <automated>cd "C:/Users/User/Desktop/Personal Project/Auto Intrinsic Value" && git log --oneline -1 && git status --short docs/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `git status docs/index.html` shows no uncommitted changes (clean working tree for that file)
    - `git log --oneline -1` shows a commit containing Phase 3 navigation changes
  </acceptance_criteria>
  <done>Changes pushed to main, GitHub Pages deployment triggered or complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify CORS and full flow on live GitHub Pages site (per D-08)</name>
  <files></files>
  <action>Human verification of live deployment. No code changes — CORS and navigation check on deployed GitHub Pages site.</action>
  <what-built>Calculator integration deployed to live GitHub Pages site. This checkpoint verifies the Render API is accessible from the deployed domain (CORS check per D-08).</what-built>
  <how-to-verify>
    1. Open the GitHub Pages URL in a browser (e.g., https://[username].github.io/[repo]/)
    2. Confirm home page loads with hero section and calculator card
    3. Click the calculator card — confirm URL changes to #calculator and iframe loads
    4. Open browser DevTools (F12) -> Console tab
    5. In the calculator, enter a stock ticker (e.g., AAPL) and click Fetch/Calculate
    6. Check DevTools console for CORS errors:
       - If NO errors and stock data appears: CORS is working. Type "approved"
       - If CORS error appears: Note the exact error message and type "cors-blocked"
    7. Also verify: click the logo to return home, test back button, test direct #calculator URL

    If CORS is blocked (D-09): The fix is updating the Flask app on Render. The `CORS(app)` call likely needs `origins="*"` or the specific GitHub Pages domain. User redeploys manually on Render after fixing.
  </how-to-verify>
  <verify>
    <automated>echo "Checkpoint: requires human verification on live GitHub Pages site"</automated>
  </verify>
  <done>User confirms calculator fetches stock data on live site without CORS errors, and full navigation flow (card click, logo home, back button, direct URL) works.</done>
  <resume-signal>Type "approved" if calculator fetches stock data successfully, or "cors-blocked" with the error message if CORS fails</resume-signal>
</task>

</tasks>

<verification>
- GitHub Pages site loads at the expected URL
- Calculator iframe loads within the page
- Calculator successfully fetches stock data from Render API (no CORS errors in console)
- Full navigation flow (card click -> calculator -> logo click -> home) works on live site
</verification>

<success_criteria>
- The live GitHub Pages site shows the portfolio with working in-page calculator navigation
- A real stock fetch (e.g., AAPL) succeeds without CORS errors in the browser console
- All three requirements (SHOW-03, NAV-01, NAV-02) are verifiably met on the deployed site
</success_criteria>

<output>
After completion, create `.planning/phases/03-calculator-integration/03-02-SUMMARY.md`
</output>
