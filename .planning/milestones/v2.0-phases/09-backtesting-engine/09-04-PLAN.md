---
phase: 09-backtesting-engine
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - scripts/pattern_scanner/split_config.py        # ONLY if cutoff revision is required
  - .planning/phases/09-backtesting-engine/09-SUMMARY.md
  - _dev/backtest_cache.json                       # generated, NOT committed (gitignored per D-06)
autonomous: false
requirements: [BT-03]
status: pending
tags: [backtest, empirical-validation, closeout, manual-run]

must_haves:
  truths:
    - "_dev/backtest_cache.json exists locally after a full --tickers all run"
    - "At least one confirmation_type in the out_of_sample.aggregates.by_confirmation_type slice has n >= 10 (BT-03 acceptance gate)"
    - "Cache header onnx_sha256 matches sha256sum models/inside_bar_v1.onnx (or is null if --no-onnx was used)"
    - "_dev/backtest_cache.json is NOT committed to the repository (gitignored)"
    - "Phase 9 SUMMARY documents: post-cutoff N per confirmation_type, wall-clock duration, R-as-return-unit interpretation of BT-03, auto_adjust=True implication on absolute prices, ticker-list cross-machine drift caveat"
    - "If post-cutoff N<10 for ALL confirmation_types: TRAIN_TEST_CUTOFF revised in scripts/pattern_scanner/split_config.py with rationale documented in SUMMARY"
  artifacts:
    - path: ".planning/phases/09-backtesting-engine/09-SUMMARY.md"
      provides: "Phase 9 closeout: empirical validation, methodology notes, hand-off to Phase 10/11"
    - path: "_dev/backtest_cache.json"
      provides: "Empirically generated backtest cache (LOCAL ONLY; not committed)"
      contains: "schema_version"
  key_links:
    - from: ".planning/phases/09-backtesting-engine/09-SUMMARY.md"
      to: "_dev/backtest_cache.json"
      via: "documents the per-confirmation_type N values from the empirical run"
      pattern: "out_of_sample.*by_confirmation_type"
---

<objective>
Empirical validation step. Run the backtester end-to-end against the live S&P 500 + 10y yfinance data, confirm BT-03's "at least one confirmation_type has N >= 10 in out_of_sample" gate is met, and write the Phase 9 SUMMARY. This plan is **autonomous: false** because it requires (a) network access to yfinance, (b) wall-clock time outside any single Claude session, and (c) human review of empirical numbers before closeout.

Purpose: close the loop between Plan 09-03's "all unit tests green" and the actual portfolio-narrative claim that the backtester produces meaningful out-of-sample statistics. This is also the moment to document the methodology trade-offs (R-multiples vs %, auto_adjust=True, ticker-list drift) so a reviewer can read the SUMMARY and trace every decision back to a source-of-truth.

Output: `_dev/backtest_cache.json` produced locally (NOT committed), `09-SUMMARY.md` recording empirical N + methodology notes, optional `split_config.py` revision IF (and only if) the empirical N is below the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-backtesting-engine/09-CONTEXT.md
@.planning/phases/09-backtesting-engine/09-RESEARCH.md
@.planning/phases/09-backtesting-engine/09-01-SUMMARY.md
@.planning/phases/09-backtesting-engine/09-02-SUMMARY.md
@.planning/phases/09-backtesting-engine/09-03-SUMMARY.md
@scripts/pattern_scanner/backtest.py
@scripts/pattern_scanner/split_config.py
@.gitignore

<interfaces>
<!-- The full run command -->
```bash
python -m scripts.pattern_scanner.backtest \
    --seed 42 \
    --tickers all \
    --out _dev/backtest_cache.json
```

<!-- Cutoff feasibility math from RESEARCH §"Cutoff Feasibility Analysis" lines 568–595 -->
- Pre-cutoff filtered detections: 4,010 (verified via models/dataset_manifest.json)
- Density: ~0.886 filtered detections / ticker / year
- Expected post-cutoff filtered total: ~1,050 detections (503 tickers × 0.886/yr × 2.357 yr)
- Per-type expectation (skewed pin/mark_up/ice_cream = 60/25/15%): pin~630, mark_up~262, ice_cream~158
- Conclusion: KEEP cutoff at 2024-01-01 unless empirical numbers diverge dramatically

<!-- Required SUMMARY contents per RESEARCH §"Risks & Gotchas" -->
1. Risk 3 (auto_adjust=True): note that prices in cache are dividend-adjusted; R-multiples are unaffected; absolute prices may differ from non-adjusted external charts.
2. Risk 7 (ticker-list drift): cache header captures live ticker list at run time; cross-machine determinism preserved only when same fetch was received.
3. D-05 R-multiples interpretation of BT-03 "avg return %" — recorded as `avg_return_r`, not percent.
4. Wall-clock numbers (per RESEARCH Assumptions A2/A3 — promote estimates to measured values).
5. Per-confirmation_type N for both `in_sample` and `out_of_sample` in both strategies.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full S&P 500 backtest and capture wall-clock timing</name>
  <read_first>
    - scripts/pattern_scanner/backtest.py (verify the CLI args list — `--seed`, `--tickers`, `--out`, `--no-onnx`, `--limit`)
    - .planning/phases/09-backtesting-engine/09-03-SUMMARY.md (any wall-clock estimate from the smoke run)
    - .planning/phases/09-backtesting-engine/09-RESEARCH.md §"ONNX Overlay Performance Estimation" lines 614–642
  </read_first>
  <files>
    - _dev/backtest_cache.json (generated; will be gitignored)
  </files>
  <action>
Execute the full backtest with explicit timing. From repo root:

```bash
mkdir -p _dev
START_TS=$(date -u +%FT%TZ)
time python -m scripts.pattern_scanner.backtest \
    --seed 42 \
    --tickers all \
    --out _dev/backtest_cache.json 2>&1 | tee _dev/backtest_run.log
END_TS=$(date -u +%FT%TZ)
echo "started=$START_TS ended=$END_TS"
```

Capture the wall-clock duration from `time` output (or compute END_TS - START_TS). Record it for use in Task 3's SUMMARY.

If wall-clock exceeds 30 minutes for the unfiltered strategy specifically:
1. Note this in the SUMMARY as a measured value (Assumption A2/A3 promotion).
2. Do NOT add ThreadPoolExecutor in this plan — defer to a follow-up refinement task only if it becomes a real blocker (per RESEARCH recommendation: "Do not preemptively parallelize").

If a partial-run failure occurs (yfinance crash, ONNX session failure, etc.):
- The per-ticker error swallow should keep the run going (Plan 09-02 mirrored gen_training_data.py's pattern).
- If ALL tickers fail (e.g., yfinance fully down), wait + retry.
- Document any anomalies in the SUMMARY's "Run anomalies" section.

**Do NOT commit `_dev/backtest_cache.json` or `_dev/backtest_run.log`.** Both should be ignored by `.gitignore` (`_dev/backtest_cache.json` was added by Plan 09-02; logs are covered by the existing `_dev/` patterns).
  </action>
  <verify>
    <automated>test -f _dev/backtest_cache.json &amp;&amp; python -c "import json, sys; d=json.load(open('_dev/backtest_cache.json')); assert d['schema_version']==1, 'schema_version mismatch'; assert 'strategies' in d and '1to2_rr_cluster_low_stop' in d['strategies'] and '1to2_rr_cluster_low_stop__unfiltered' in d['strategies'], 'missing strategy keys'; assert 'in_sample' in d['strategies']['1to2_rr_cluster_low_stop'] and 'out_of_sample' in d['strategies']['1to2_rr_cluster_low_stop'], 'missing sample blocks'; print('schema ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `_dev/backtest_cache.json` exists and is non-empty
    - File is valid JSON with `schema_version=1`, both strategy keys, both sample blocks per strategy
    - Cache header contains: `generated_at`, `train_test_cutoff`, `seed=42`, `ticker_list`, `ticker_count`, `onnx_sha256`
    - `! git ls-files --error-unmatch _dev/backtest_cache.json 2>/dev/null` (file is NOT tracked)
    - `grep -q "_dev/backtest_cache.json" .gitignore` (still gitignored)
    - Wall-clock duration recorded (capture from `time` output)
  </acceptance_criteria>
  <done>Empirical run complete; cache JSON exists locally with valid structure; wall-clock duration captured for SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human checkpoint — verify BT-03 N >= 10 acceptance gate</name>
  <what-built>
    Plan 09-03 wired the full backtester. Task 1 above ran it end-to-end against the live S&P 500. The acceptance gate for BT-03 is empirical: at least one `confirmation_type` in the out_of_sample slice must have `n >= 10`.
  </what-built>
  <how-to-verify>
    Run this inspection command to print the relevant numbers:

    ```bash
    python -c "
import json
d = json.load(open('_dev/backtest_cache.json'))
print('=== Header ===')
print(f'  generated_at:       {d[\"generated_at\"]}')
print(f'  train_test_cutoff:  {d[\"train_test_cutoff\"]}')
print(f'  seed:               {d[\"seed\"]}')
print(f'  ticker_count:       {d[\"ticker_count\"]}')
print(f'  onnx_sha256:        {d[\"onnx_sha256\"]}')
for sname in ('1to2_rr_cluster_low_stop', '1to2_rr_cluster_low_stop__unfiltered'):
    s = d['strategies'][sname]
    print(f'\n=== {sname} ===')
    for sample in ('in_sample', 'out_of_sample'):
        block = s[sample]
        all_cell = block['aggregates'].get('all', {})
        print(f'  [{sample}] n={all_cell.get(\"n\", 0)}  win_rate={all_cell.get(\"win_rate\")}  avg_return_r={all_cell.get(\"avg_return_r\")}')
        print(f'  [{sample}] by_confirmation_type:')
        for k, c in sorted(block['aggregates'].get('by_confirmation_type', {}).items()):
            print(f'    {k}: n={c[\"n\"]} n_resolved={c[\"n_resolved\"]} win_rate={c[\"win_rate\"]} avg_return_r={c[\"avg_return_r\"]:.3f}')
"
    ```

    **Verify each item below:**

    1. The `1to2_rr_cluster_low_stop` strategy's `out_of_sample.by_confirmation_type` has at least ONE row with `n >= 10`. (Per RESEARCH cutoff-feasibility math, expect pin>>10, mark_up>10, ice_cream>10.)
    2. The `1to2_rr_cluster_low_stop__unfiltered` strategy's `out_of_sample` has substantially MORE detections than `1to2_rr_cluster_low_stop` (filter ablation narrative).
    3. Total wall-clock time (Task 1 captured) is reasonable (<60 min preferred; <30 min hoped for the filtered run; unfiltered may be longer).
    4. `onnx_sha256` is a 64-character hex string (the model was loaded; not null).
    5. No suspicious all-zero or all-null values that suggest a wiring failure.

    **Decision branches:**
    - **GREEN (BT-03 met):** Proceed to Task 3 (SUMMARY). No code changes.
    - **YELLOW (one or two confirmation_types miss N>=10 but at least one clears it):** BT-03 still passes. Note in SUMMARY which types were thin; Phase 11 UI will surface this.
    - **RED (ALL confirmation_types in out_of_sample have n<10 in BOTH strategies):** Per D-10, propose a revised TRAIN_TEST_CUTOFF and update `scripts/pattern_scanner/split_config.py`. This will trigger a re-run of Task 1. Document the rationale + the Phase 8 retrain implication in the SUMMARY.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - "approved — proceed to SUMMARY" (GREEN/YELLOW path)
    - "revise cutoff to YYYY-MM-DD" (RED path; specify new date)
    - "investigate {issue}" (any wiring concern from items 1–5 above)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write Phase 9 SUMMARY with empirical results, methodology notes, and Phase 10/11 hand-off</name>
  <read_first>
    - .planning/phases/08-training-pipeline/08-05-SUMMARY.md (Phase 8 closeout structure to mirror)
    - .planning/phases/09-backtesting-engine/09-CONTEXT.md (D-01..D-17 — canonical decision references)
    - .planning/phases/09-backtesting-engine/09-RESEARCH.md §"Risks & Gotchas" lines 921–966 (Risk 3, 4, 7 must appear in SUMMARY)
    - _dev/backtest_cache.json (the empirical numbers)
    - .planning/phases/09-backtesting-engine/09-01-SUMMARY.md, 09-02-SUMMARY.md, 09-03-SUMMARY.md (chain history)
  </read_first>
  <files>
    - .planning/phases/09-backtesting-engine/09-SUMMARY.md (new file)
  </files>
  <action>
Create `.planning/phases/09-backtesting-engine/09-SUMMARY.md` with the following sections (mirror Phase 8's closeout SUMMARY structure):

```markdown
# Phase 9 — Backtesting Engine — SUMMARY

**Closed:** {YYYY-MM-DD}
**Plans:** 4 (09-01, 09-02, 09-03, 09-04)
**Requirements satisfied:** BT-01, BT-02, BT-03

## Deliverables

- `scripts/pattern_scanner/backtest.py` — pure-function core (`simulate_trade`, `aggregate`) + orchestrator (`main`) + ONNX overlay (`_load_onnx_session`, `_score_detection`).
- 8 unit tests across 6 test files (Plan 09-01 + 09-02 + 09-03), all green: stop_first, target_first, intrabar_pessimistic, open_outcome, aggregate_groupings, cutoff_isolation, unfiltered_superset, yolo_conf_null_when_onnx_missing, plus the parametrized `test_backtest_cli` security suite.
- `_dev/backtest_cache.json` produced empirically; gitignored (D-06).
- `.gitignore` updated to exclude `_dev/backtest_cache.json`.

## Empirical Results (BT-03 acceptance evidence)

**Run details:**
- Date: {YYYY-MM-DD}
- Seed: 42
- Ticker count: {N}
- Wall-clock: {HH:MM}
- ONNX SHA-256: {hex} (model: `models/inside_bar_v1.onnx`)
- TRAIN_TEST_CUTOFF: {value} {(REVISED from prior value if applicable)}

**Out-of-sample N by confirmation_type (filtered strategy):**

| confirmation_type | n | n_resolved | win_rate | avg_return_r |
|-------------------|---|------------|----------|--------------|
| pin | {N} | {N} | {%} | {R} |
| mark_up | {N} | {N} | {%} | {R} |
| ice_cream | {N} | {N} | {%} | {R} |

**BT-03 gate (at least one type with n >= 10):** {PASS / FAIL}

**Filter ablation (out_of_sample, all-cell):**

| strategy | n | win_rate | avg_return_r |
|----------|---|----------|--------------|
| 1to2_rr_cluster_low_stop | {N} | {%} | {R} |
| 1to2_rr_cluster_low_stop__unfiltered | {N} | {%} | {R} |

Difference: trend filters change win_rate from {X}% (unfiltered) to {Y}% (filtered) — quantified, not asserted.

## Methodology Notes (must read before consuming the cache)

### R-multiples are the unit, not percent (D-05)

BT-03 says "average return %". Phase 9 reports `avg_return_r` (R-multiples) — the methodologically clean unit for a fixed 1:2 R:R rule. Stop = -1.0R, target = +2.0R. Open trades carry an unrealized R = (last_close - entry) / risk. This is a deliberate reinterpretation of BT-03; rationale: R-multiples normalize across ticker price levels and align with how a discretionary trader using this rule would frame returns.

### Prices are dividend-adjusted (Risk 3 / Pitfall 5)

`yfinance(auto_adjust=True)` is inherited from Phase 7. Absolute `entry_price`, `stop_price`, `target_price`, `exit_price` in the cache are dividend-adjusted, which differs from TradingView's default split-adjusted-only view on dividend-paying tickers. R-multiples are unaffected (ratios within the same series).

### Pessimistic intrabar resolution (D-03)

When a single bar's `[low, high]` contains both stop and target, stop wins. This is the standard backtester convention without intraday data — avoids overstating win-rate.

### Ticker-list drift caveat (Risk 7)

The reproducibility tuple `(seed, ticker_list, TRAIN_TEST_CUTOFF, onnx_sha256)` is captured in the cache header. Cross-machine determinism is guaranteed only when yfinance returns identical OHLC series. Same caveat as Phase 8's concat_sha256 cross-machine drift.

### `yolo_conf` is a quality signal, not a P&L signal (D-13)

The model was trained to mimic the algorithmic detector. `yolo_conf` measures chart-textbook-ness, not trade-success probability. Raw float stored per-detection; tier thresholds are a Phase 11 UI decision once the post-cutoff conf-distribution histogram is visible.

### "open" outcomes can be very old (Risk 4)

D-02 explicitly chose no timeout. A 2014 detection still marked `open` in 2026 has `hold_days ~ 4400`. The schema field disambiguates; Phase 11 UI may want to gate "still pending" on `hold_days < some_recent_threshold`.

## Phase 10 / 11 Hand-off

**For Phase 10 (Batch Pipeline):**
- Phase 10 does NOT consume `_dev/backtest_cache.json` — that file is local-only / portfolio-narrative-only.
- Phase 10's nightly inference runs on detection + ONNX inference, NOT on backtesting. Backtest cache is regenerated manually when methodology changes.

**For Phase 11 (Frontend):**
- The backtest cache lives at `_dev/backtest_cache.json` LOCALLY (gitignored). Phase 11 must add a small build step (probably in the deploy script) to copy/transform it into a frontend-readable JSON at `docs/projects/patterns/backtest_stats.json` (or similar).
- Phase 11 stat cards must read from `out_of_sample`, NOT `in_sample` — the integrity story.
- Confidence-tier thresholds (green/yellow/red badges): inspect the `out_of_sample.detections[].yolo_conf` distribution from this cache to choose breakpoints. RESEARCH suggests starting with the 0.3 floor from Phase 8's CONFIDENCE_FLOOR but the histogram is the source of truth.

## Risks Documented (per RESEARCH §"Risks & Gotchas")

| Risk | Status | Note |
|------|--------|------|
| Risk 1 (numpy.float64 leak) | mitigated | All numeric fields wrapped in `float()` at write-time; verified by isinstance asserts in `test_backtest_simulate_trade.py`. |
| Risk 2 (Timestamp leak) | mitigated | All date fields use `df.index[i].strftime("%Y-%m-%d")`; verified by regex assert. |
| Risk 3 (auto_adjust=True) | documented | See Methodology Notes above. |
| Risk 4 (open detections walk forever) | accepted | D-02 explicit choice. Documented for Phase 11 UI. |
| Risk 5 (ONNX session memory) | accepted | onnxruntime production-tested at this scale. |
| Risk 6 (detect ordering not stable) | mitigated | Records sorted by `(ticker, confirmation_date, mother_bar_index)` before serialization. |
| Risk 7 (ticker-list drift) | documented | Cache header captures live list. |

## Cutoff Decision Log

{One of:}
- **No revision.** TRAIN_TEST_CUTOFF held at 2024-01-01. Empirical N comfortably clears BT-03.
- **Revised** from 2024-01-01 to {NEW_DATE}. Rationale: {explain}. Phase 8 implication: trained model used data through {OLD_DATE} which is now in `out_of_sample` per the new boundary. {Either} accepted as documented limitation {or} flagged for Phase 8 retrain note.

## Open Items / Follow-ups

- {Any wall-clock concerns; whether ThreadPoolExecutor refinement is warranted for unfiltered runs}
- {Any per-ticker errors during the empirical run worth investigating}
- {Phase 11 build-step skeleton — out of scope here}
```

Fill in `{...}` placeholders from the actual JSON output and `time` measurements captured in Task 1. If the cutoff was revised in Task 2's RED path, also update `scripts/pattern_scanner/split_config.py` and document under "Cutoff Decision Log".
  </action>
  <verify>
    <automated>test -f .planning/phases/09-backtesting-engine/09-SUMMARY.md &amp;&amp; grep -q "BT-03" .planning/phases/09-backtesting-engine/09-SUMMARY.md &amp;&amp; grep -q "R-multiples" .planning/phases/09-backtesting-engine/09-SUMMARY.md &amp;&amp; grep -q "auto_adjust" .planning/phases/09-backtesting-engine/09-SUMMARY.md &amp;&amp; grep -q "Risk 7" .planning/phases/09-backtesting-engine/09-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/09-backtesting-engine/09-SUMMARY.md` exists
    - Contains all required sections: Deliverables, Empirical Results, Methodology Notes (R-multiples, auto_adjust, pessimistic intrabar, ticker drift, yolo_conf, open outcomes), Phase 10/11 Hand-off, Risks Documented, Cutoff Decision Log
    - Empirical N table is filled with actual numbers (not placeholder text)
    - Wall-clock duration recorded
    - BT-03 PASS/FAIL stated explicitly
    - If cutoff was revised: `split_config.py` change documented under "Cutoff Decision Log"
    - `_dev/backtest_cache.json` is still NOT committed: `! git ls-files --error-unmatch _dev/backtest_cache.json 2>/dev/null`
    - All 8 D-17 tests still green: `pytest tests/test_backtest_*.py -q` exits 0
    - Full repo suite still green: `pytest tests/ -q` exits 0
    - requirements.txt unchanged: `git diff --stat requirements.txt` shows no diff
  </acceptance_criteria>
  <done>Phase 9 SUMMARY committed; empirical BT-03 evidence recorded; methodology notes complete; ready for Phase 10/11.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live yfinance API | The empirical run pulls from the live API. Treated as untrusted from a path-traversal standpoint, but the data path (numeric OHLC) has no injection surface. Inherited from Phase 7+8. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-01, T-9-03, T-9-04 | (inherited) | (inherited from Plans 09-02, 09-03) | inherited | No new threat surface introduced by this plan. The empirical run uses the same code paths whose threats are already mitigated upstream. |

This plan is operational — running existing code + writing prose. No new code attack surface.
</threat_model>

<verification>
- `_dev/backtest_cache.json` exists locally with valid structure (Task 1)
- BT-03 empirical gate met: at least one confirmation_type has n >= 10 in out_of_sample (Task 2 human-verified)
- `09-SUMMARY.md` exists with all required sections including methodology notes for Risk 3, 4, 7 (Task 3)
- Cache file is NOT committed: `! git ls-files --error-unmatch _dev/backtest_cache.json 2>/dev/null` succeeds
- All 8 D-17 tests still green
- Full pytest suite green
- requirements.txt unchanged
</verification>

<success_criteria>
- [x] Empirical run produces a valid `_dev/backtest_cache.json`
- [x] BT-03 N>=10 gate verified by human (or cutoff revised + re-run if not)
- [x] 09-SUMMARY.md documents methodology notes (R-multiples, auto_adjust, pessimistic intrabar, ticker drift, yolo_conf, open semantics)
- [x] Phase 10 / 11 hand-off section written
- [x] Cache file gitignored and NOT committed
- [x] All 8 D-17 unit tests still pass
- [x] No torch / ultralytics added to requirements.txt
</success_criteria>

<output>
Phase 9 closeout. Update `.planning/STATE.md`:
- `progress.completed_phases` += 1
- `progress.completed_plans` += 4
- `current_position` advances to Phase 10
- Add a "Phase 9 complete" entry to the Recent Decisions / Notes section, including: empirical BT-03 result, R-multiples interpretation noted, cutoff status (held/revised), wall-clock measured

ROADMAP.md: mark Phase 9 as `[x]` complete with completion date.
</output>